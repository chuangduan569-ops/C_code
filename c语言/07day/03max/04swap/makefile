main : main.o swap.o
	gcc -o $@ $^
#在makefile文件中可以引用变量,使用$进行引用
# $@	目标文件(上一条语句 :以左)		(生成的文件)
# &^	依赖文件(上一条语句 :以右)
clean :
	rm -rf *.o main
#	rm -rf $@ &^ 不要这样使用,因为 $@ 表示上一条语句 :以左 也就是clean
#	rm -rf $@ &^ 不要这样使用,因为 $@ 表示上一条语句 :以右 也就是空的	
	

